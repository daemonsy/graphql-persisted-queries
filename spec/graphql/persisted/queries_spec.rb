RSpec.describe Graphql::Persisted::Queries do
  it "has a version number" do
    expect(Graphql::Persisted::Queries::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
